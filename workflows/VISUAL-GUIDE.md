# Visual Guide: Hello World Workflow

A step-by-step visual walkthrough of your first n8n workflow.

---

## The Complete Workflow

```
┌─────────────────────────────────────────────────────────────────────┐
│                       HELLO WORLD WORKFLOW                          │
└─────────────────────────────────────────────────────────────────────┘

     START                   PROCESS                  FORMAT
       │                       │                        │
       ▼                       ▼                        ▼
  ┌─────────┐            ┌─────────┐             ┌─────────┐
  │         │            │         │             │         │
  │  👆     │   ────────▶│  💻     │  ──────────▶│  📋     │
  │ Manual  │            │  Code   │             │  Set    │
  │ Trigger │            │  Node   │             │  Node   │
  │         │            │         │             │         │
  └─────────┘            └─────────┘             └─────────┘

  Click to     →      Generate message    →    Clean output
  start                with timestamp          structure
```

---

## Node-by-Node Breakdown

### 1️⃣ Manual Trigger Node

```
┌────────────────────────────────────┐
│  When clicking 'Test workflow'     │
│                                    │
│  Type: n8n-nodes-base.manualTrigger│
│  Position: Left side (240, 300)   │
│                                    │
│  👆 Click "Test workflow" to start │
└────────────────────────────────────┘
          │
          │ Outputs: Empty trigger signal
          ▼
```

**What happens**:
- You click "Test workflow" button
- Workflow starts
- No data output (just triggers next node)

**Visual in n8n**:
```
┌──────────────────────┐
│ 👆                   │
│ When clicking        │
│ 'Test workflow'      │
└──────────────────────┘
```

---

### 2️⃣ Code Node (Generate Message)

```
┌────────────────────────────────────┐
│  Generate Message                  │
│                                    │
│  Type: n8n-nodes-base.code         │
│  Language: JavaScript              │
│  Position: Middle (460, 300)       │
└────────────────────────────────────┘
          │
          │ Inputs: Trigger signal
          │
          │ Processing:
          │   const currentTime = new Date().toLocaleString();
          │   const message = `Hello from n8n! 👋`;
          │
          │   return [{
          │     json: {
          │       message: message,
          │       timestamp: currentTime,
          │       status: 'success',
          │       tip: 'You just ran your first n8n workflow!'
          │     }
          │   }];
          │
          │ Outputs: JSON object
          ▼

┌─────────────────────────────────────────────────┐
│ OUTPUT DATA:                                    │
├─────────────────────────────────────────────────┤
│ {                                               │
│   "message": "Hello from n8n! 👋",              │
│   "timestamp": "11/24/2025, 3:30:00 PM",       │
│   "status": "success",                          │
│   "tip": "You just ran your first workflow!"   │
│ }                                               │
└─────────────────────────────────────────────────┘
```

**What happens**:
- Receives trigger
- Runs JavaScript code
- Gets current time
- Creates greeting message
- Returns JSON with 4 fields

**Visual in n8n**:
```
┌──────────────────────┐
│ 💻                   │
│ Generate Message     │
│                      │
│ [Code: JavaScript]   │
└──────────────────────┘
```

---

### 3️⃣ Set Node (Format Output)

```
┌────────────────────────────────────┐
│  Format Output                     │
│                                    │
│  Type: n8n-nodes-base.set          │
│  Operation: Transform data         │
│  Position: Right (680, 300)        │
└────────────────────────────────────┘
          │
          │ Inputs: JSON from Code node
          │
          │ Transformations:
          │   greeting  = {{ $json.message }}
          │   time      = {{ $json.timestamp }}
          │   result    = {{ $json.status }}
          │
          │ Outputs: Cleaned JSON
          ▼

┌─────────────────────────────────────────────────┐
│ FINAL OUTPUT:                                   │
├─────────────────────────────────────────────────┤
│ {                                               │
│   "greeting": "Hello from n8n! 👋",             │
│   "time": "11/24/2025, 3:30:00 PM",            │
│   "result": "success"                           │
│ }                                               │
└─────────────────────────────────────────────────┘
```

**What happens**:
- Receives JSON from Code node
- Extracts specific fields using expressions
- Renames them for clarity
- Returns clean, formatted output

**Visual in n8n**:
```
┌──────────────────────┐
│ 📋                   │
│ Format Output        │
│                      │
│ [3 assignments]      │
└──────────────────────┘
```

---

## Data Flow Animation

### Step 1: Click Test Workflow

```
┌──────────────┐
│ 👆 CLICK!    │
│ Test workflow│
└──────────────┘
       │
       │ Signal sent
       ▼
┌──────────────┐
│ Manual       │ ← You are here
│ Trigger      │
│ ✓ Executed   │
└──────────────┘
```

### Step 2: Code Executes

```
┌──────────────┐        ┌──────────────┐
│ Manual       │ ─────▶ │ Code Node    │
│ Trigger      │        │ ⚙️ Running... │
│ ✓ Executed   │        └──────────────┘
└──────────────┘               │
                               │ Generates:
                               │ {
                               │   message: "Hello..."
                               │   timestamp: "11/24..."
                               │   status: "success"
                               │   tip: "You just..."
                               │ }
                               ▼
                        ┌──────────────┐
                        │ Code Node    │
                        │ ✓ Executed   │
                        └──────────────┘
```

### Step 3: Data Formatted

```
┌──────────────┐        ┌──────────────┐        ┌──────────────┐
│ Manual       │ ─────▶ │ Code Node    │ ─────▶ │ Set Node     │
│ Trigger      │        │ ✓ Executed   │        │ ⚙️ Formatting │
│ ✓ Executed   │        └──────────────┘        └──────────────┘
└──────────────┘                                        │
                                                        │ Transforms to:
                                                        │ {
                                                        │   greeting: "Hello..."
                                                        │   time: "11/24..."
                                                        │   result: "success"
                                                        │ }
                                                        ▼
                                                 ┌──────────────┐
                                                 │ Set Node     │
                                                 │ ✓ Executed   │
                                                 └──────────────┘
```

### Step 4: Complete!

```
┌──────────────┐        ┌──────────────┐        ┌──────────────┐
│ Manual       │ ─────▶ │ Code Node    │ ─────▶ │ Set Node     │
│ Trigger      │        │ ✓ Executed   │        │ ✓ Executed   │
│ ✓ Executed   │        └──────────────┘        └──────────────┘
└──────────────┘

🎉 WORKFLOW COMPLETE! 🎉

Final output visible in n8n UI
Click on "Set Node" to see result
```

---

## What You'll See in n8n

### Canvas View

```
┌────────────────────────────────────────────────────────────┐
│ n8n - Workflow: Hello World - Simple Demo                 │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  [Test workflow] [Save] [⋮ Menu]                          │
│                                                            │
│                                                            │
│     ┌────────┐         ┌────────┐         ┌────────┐     │
│     │   👆   │         │   💻   │         │   📋   │     │
│     │ Manual │────────▶│  Code  │────────▶│  Set   │     │
│     │Trigger │         │  Node  │         │  Node  │     │
│     └────────┘         └────────┘         └────────┘     │
│                                                            │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### After Execution

```
┌────────────────────────────────────────────────────────────┐
│ n8n - Workflow: Hello World - Simple Demo                 │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Execution successful! ✓                                   │
│                                                            │
│     ┌────────┐         ┌────────┐         ┌────────┐     │
│     │   👆   │         │   💻   │         │   📋   │     │
│     │ Manual │────────▶│  Code  │────────▶│  Set   │     │
│     │Trigger │   1     │  Node  │    1    │  Node  │     │
│     └────────┘         └────────┘         └────────┘     │
│        ✓                   ✓                   ✓          │
│                                                            │
│  Items: 1                                                  │
│  Duration: 0.12s                                           │
└────────────────────────────────────────────────────────────┘

Click on any node to see its output data →
```

### Output Panel (Click on Set Node)

```
┌────────────────────────────────────────┐
│ Set Node - Output                      │
├────────────────────────────────────────┤
│ Item 1 of 1                            │
├────────────────────────────────────────┤
│                                        │
│ {                                      │
│   "greeting": "Hello from n8n! 👋",    │
│   "time": "11/24/2025, 3:30:00 PM",   │
│   "result": "success"                  │
│ }                                      │
│                                        │
└────────────────────────────────────────┘
```

---

## Key Visual Elements in n8n

### Node Status Indicators

```
┌────────┐
│   ⚪   │  Not executed yet (gray)
└────────┘

┌────────┐
│   ⚙️   │  Currently executing (spinning)
└────────┘

┌────────┐
│   ✓    │  Successfully executed (green checkmark)
└────────┘

┌────────┐
│   ❌   │  Error (red X)
└────────┘
```

### Connection Lines

```
┌────────┐ ──────▶ ┌────────┐
│ Node A │  Data   │ Node B │
└────────┘  Flow   └────────┘
```

- **Solid line**: Active connection
- **Number on line**: Item count passing through
- **Click line**: See data being passed

### Node Colors

- **Purple/Pink**: Trigger nodes
- **Blue**: Action nodes
- **Green**: Success state
- **Red**: Error state
- **Gray**: Not executed

---

## Interactive Elements

### Clicking Nodes

```
Click on node → Opens side panel with:
├─ Node settings
├─ Output data
├─ Execution time
└─ Debug info
```

### Hovering Over Connections

```
Hover on line between nodes → Shows:
├─ Number of items passed
├─ Data preview
└─ Connection type (main/error)
```

### Test Workflow Button

```
┌─────────────────┐
│ Test workflow ▶ │ ← Click here to run!
└─────────────────┘
```

---

## Common UI Elements

### Top Bar

```
[← Back] [Workflow Name] [Test workflow ▶] [Save] [⋮]
```

### Node Panel (Left Side)

```
┌──────────────┐
│ 🔍 Search    │
├──────────────┤
│ Triggers     │
│ Actions      │
│ Utilities    │
│ AI           │
│ ...          │
└──────────────┘
```

### Output Panel (Right Side)

```
┌──────────────┐
│ Execution #1 │
├──────────────┤
│ Node output  │
│ JSON view    │
│ Table view   │
└──────────────┘
```

---

## Keyboard Shortcuts

```
Ctrl + Enter  = Test workflow
Ctrl + S      = Save workflow
Ctrl + Z      = Undo
Ctrl + Y      = Redo
Delete        = Delete selected node
Ctrl + C      = Copy node
Ctrl + V      = Paste node
```

---

## Tips for First-Time Users

### 1. Always Save!

```
┌──────────────────────┐
│  Remember to save!   │
│  [Save] button       │
│  or Ctrl + S         │
└──────────────────────┘
```

### 2. Check Each Node

```
After execution:
1. Click on each node
2. View the output
3. Understand the data flow
```

### 3. Experiment!

```
Try changing:
- Message text in Code node
- Field names in Set node
- Add more fields
- Remove nodes and see what happens
```

### 4. Read Error Messages

```
If something fails:
1. Look for red ❌ on nodes
2. Click the node
3. Read error message in side panel
4. Fix and retry
```

---

## Next Steps

### Customize This Workflow

1. **Change the greeting**:
   - Edit Code node
   - Modify `message` variable
   - Click "Test workflow"

2. **Add more data**:
   - Add fields to JSON return
   - Update Set node assignments
   - See new output

3. **Change the trigger**:
   - Delete Manual Trigger
   - Add Webhook Trigger
   - Make it accessible via HTTP!

### Build Your Second Workflow

Try these next:
- API fetch workflow
- Slack notification
- Data transformation
- Scheduled task

---

**Visual guide complete! Now import and try it yourself! 🚀**
